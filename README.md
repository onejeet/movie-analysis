# Multilevel Filtering in React 

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Reproduce on localhost
- extract the zip file
- get inside the directory
- run `npm install`
- run `npm start`

